//MANUAL CAMERA CONTROL CODE (Using Arrow Keys)
// Create Controls Variable >>> let controls;
//Add this code to a create() function
// const cursors = this.input.keyboard.createCursorKeys();
// controls = new Phaser.Cameras.Controls.FixedKeyControl({
//   camera: camera,
//   left: cursors.left,
//   right: cursors.right,
//   up: cursors.up,
//   down: cursors.down,
//   speed: 0.5,
// });

//Add this code to the update() function with delta passed as a second parameter
//controls.update(delta);

//visual representation of tiles with collision
// const debugGraphics = this.add.graphics().setAlpha(0.75);
// [Name of Map Layer variable].renderDebug(debugGraphics, {
//   tileColor: null, // Color of non-colliding tiles
//   collidingTileColor: new Phaser.Display.Color(243, 134, 48, 255), // Color of colliding tiles
//   faceColor: new Phaser.Display.Color(40, 39, 37, 255), // Color of colliding face edges
// });
